export interface loginInfoType {
    username: string,
    password: string
}

export interface meetingRoomListParamsType {
    pageNo: number,
    pageSize: number,
    name: string,
    capacity: string,
    equipment: string,
}
